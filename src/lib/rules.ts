import type { Player, SquadPlayer } from "./types";
export type RuleError = { code:string; reason:string; details:string };
const positions={GKP:2,DEF:5,MID:5,FWD:3};
export function validateSquad(players:Player[], maxValue?:number):RuleError | null { if(players.length!==15)return {code:"SQUAD_SIZE",reason:"A squad must contain 15 players.",details:`Received ${players.length}.`}; for(const [position,count] of Object.entries(positions)){const actual=players.filter(p=>p.position===position).length;if(actual!==count)return {code:"POSITION_QUOTA",reason:`A squad needs ${count} ${position}.`,details:`Received ${actual}.`}} const clubs=new Map<number,number>();for(const p of players){clubs.set(p.teamId,(clubs.get(p.teamId)??0)+1);if(clubs.get(p.teamId)!>3)return {code:"MAX_PLAYERS_PER_CLUB",reason:"Maximum three players from a club.",details:`${p.team} would have ${clubs.get(p.teamId)} players.`}} if(maxValue!==undefined&&players.reduce((sum,p)=>sum+p.price,0)>maxValue+0.001)return {code:"OVER_BUDGET",reason:"The squad exceeds available budget.",details:"Reduce total squad cost."};return null; }
export function validateLineup(players:SquadPlayer[]):RuleError | null { const starters=players.filter(p=>p.multiplier>0); if(starters.length!==11)return {code:"STARTING_XI_SIZE",reason:"Starting XI must contain exactly 11 players.",details:`Received ${starters.length}.`}; const g=starters.filter(p=>p.position==="GKP").length,d=starters.filter(p=>p.position==="DEF").length,m=starters.filter(p=>p.position==="MID").length,f=starters.filter(p=>p.position==="FWD").length;if(g!==1||d<3||m<2||f<1)return {code:"INVALID_FORMATION",reason:"Lineup does not meet position requirements.",details:`GKP ${g}, DEF ${d}, MID ${m}, FWD ${f}.`};const cap=players.filter(p=>p.isCaptain),vice=players.filter(p=>p.isViceCaptain);if(cap.length!==1||vice.length!==1||cap[0].id===vice[0].id)return {code:"INVALID_CAPTAINCY",reason:"Select distinct captain and vice captain.",details:"Exactly one of each is required."};return null; }
